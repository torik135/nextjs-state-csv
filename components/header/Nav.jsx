import NavItem from "./NavItem"

const Nav = () => {
    return (
        <nav className="nav">
            <NavItem/>
            <NavItem/>
            <NavItem/>
        </nav>
    )
}

export default Nav
